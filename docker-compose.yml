services:
  postgres:
    image: postgres:16-alpine
    container_name: walkquest-postgres
    environment:
      POSTGRES_DB: walkquest
      POSTGRES_USER: walkquest
      POSTGRES_PASSWORD: walkquest
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
